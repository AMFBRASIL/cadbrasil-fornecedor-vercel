# ==============================================================================
# CADBRASIL FRONTEND — Variáveis de Ambiente (Vercel / local)
# ==============================================================================

# URL pública do backend Next.js (projeto Vercel separado)
# Obrigatório em produção — sem isso as chamadas /api falham no browser
# Obrigatório incluir https:// (sem protocolo vira path relativo no browser)
VITE_API_URL=https://seu-backend.vercel.app

# Exemplo local (proxy Vite encaminha /api → :3001; deixe vazio ou omita):
# VITE_API_URL=
